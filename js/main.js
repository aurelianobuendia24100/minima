$(document).ready(function(){
  var $button = $('#button'),
    $name = $('#name'),
    $address = $('#address'),
    $number = $('#number'),
    $form = $('form')
  $form.submit(function(e) {
    e.preventDefault();

  });
});
